<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CinemaBookingSystemFE"
        android:usesCleartextTraffic="true">
        
        <!-- Splash Activity (LAUNCHER) -->
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="false"
            android:theme="@style/Theme.CinemaBookingSystemFE" />
        
        <!-- Login Activity - First screen -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.CinemaBookingSystemFE">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Main Activity - After login -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.CinemaBookingSystemFE" />
        
        <!-- Authentication Activities -->
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.CinemaBookingSystemFE" />
        
        <activity
            android:name=".ui.auth.ForgotPasswordActivity"
            android:exported="false"
            android:theme="@style/Theme.CinemaBookingSystemFE" />
        
        <!-- TODO: Other activities will be added when modules are implemented -->
        
        <!-- Search Movie Activity -->
        <activity
            android:name=".ui.moviedetail.SearchMovieActivity"
            android:exported="false"
            android:screenOrientation="portrait"/>
        
        <!-- TODO: Thêm các Activities khác khi implement -->
        <!-- MovieDetailActivity, ProfileEditActivity, etc. -->
        
    </application>

</manifest>